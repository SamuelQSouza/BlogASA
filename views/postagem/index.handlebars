<div class="card">
    <div class="card-body">
        <h1>{{postagem.titulo}}</h1>
        <hr>
        <small>Data publicação: {{postagem.date}} </small>
        <hr>
        {{#each postagem.conteudo}}
            <p>{{this}}</p> 
        {{/each}}
        
    </div>
</div>


